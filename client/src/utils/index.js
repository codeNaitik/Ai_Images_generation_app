import {surpriseMePrompts} from '../constants'

export function getRandomPrompt(prompt){

    //to create rnadom ai images
    const randomIndex = Math.floor(Math.random()*
    surpriseMePrompts.length);

    
}